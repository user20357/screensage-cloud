# Minimal requirements for quick testing
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Basic image processing (without heavy ML libraries)
Pillow==10.1.0
pytesseract==0.3.10

# Optional: OpenAI (can work without it)
openai==1.3.5

# Basic HTTP client
requests==2.31.0